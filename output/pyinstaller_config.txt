[OPTIONS]
# 指定打包的主程序文件
main = src/ui/pyqt6/main.py

# 指定打包后的可执行文件名称
name = EasyKiConverter

# 指定打包后的可执行文件图标（需要根据实际路径调整）
# icon = src/ui/pyqt6/resources/icon.ico

# 打包为一个单独的可执行文件
onefile = True

# 禁用控制台窗口（GUI应用）
noconsole = True

# 指定额外的数据文件（如果需要）
# datas = src/ui/pyqt6/resources;resources

# 指定隐藏的导入模块
hiddenimports = [
    PyQt6.QtCore,
    PyQt6.QtGui,
    PyQt6.QtWidgets,
    pandas,
    openpyxl,
    requests,
    pydantic
]

# 指定排除的模块（减小打包体积）
excludes = [
    tkinter,
    matplotlib,
    scipy,
    numpy.random._pickle
]

# 指定包含的模块路径
paths = [
    src,
    src/core,
    src/ui,
    src/ui/pyqt6,
    src/ui/pyqt6/utils,
    src/ui/pyqt6/widgets,
    src/ui/pyqt6/workers
]